Blockly.JavaScript['sr_unique'] = function(block) {
    var code = '\n';
    return [code, Blockly.Python.ORDER_ATOMIC];
  };

Blockly.JavaScript['sr_name'] = function(block) {
    var code = '\n';
    return [code, Blockly.Python.ORDER_ATOMIC];
  };

Blockly.JavaScript['sr_is_in'] = function(block) {
    var code = '\n';
    return [code, Blockly.Python.ORDER_ATOMIC];
  };

Blockly.JavaScript['sr_tolist'] = function(block) {
    var code = '\n';
    return [code, Blockly.Python.ORDER_ATOMIC];
  };

  Blockly.JavaScript['sr_index()'] = function(block) {
    var code = '\n';
    return [code, Blockly.Python.ORDER_ATOMIC];
  };

  Blockly.JavaScript['sr_index[]'] = function(block) {
    var code = '\n';
    return [code, Blockly.Python.ORDER_ATOMIC];
  };

  Blockly.JavaScript['sr_condition[]'] = function(block) {
    var code = '\n';
    return [code, Blockly.Python.ORDER_ATOMIC];
  };

  //db

  Blockly.JavaScript['db_autocommit'] = function (block) {
    // TODO: Assemble .JavaScript into code variable.
    var code = '\n';
    return code;
  };

  Blockly.JavaScript['db_deletedb'] = function (block) {
    // TODO: Assemble .JavaScript into code variable.
    var code = '\n';
    return code;
  };

  Blockly.JavaScript['db_select3'] = function (block) {
    // TODO: Assemble .JavaScript into code variable.
    var code = '\n';
    return code;
  };

  Blockly.JavaScript['db_select4'] = function (block) {
    // TODO: Assemble .JavaScript into code variable.
    var code = '\n';
    return code;
  };

  Blockly.JavaScript['db_select5'] = function (block) {
    // TODO: Assemble .JavaScript into code variable.
    var code = '\n';
    return code;
  };

  Blockly.JavaScript['db_select6'] = function (block) {
    // TODO: Assemble .JavaScript into code variable.
    var code = '\n';
    return code;
  };

  Blockly.JavaScript['db_select7'] = function (block) {
    // TODO: Assemble .JavaScript into code variable.
    var code = '\n';
    return code;
  };